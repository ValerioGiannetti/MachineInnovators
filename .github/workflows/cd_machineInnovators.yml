##Questo mi permette di creare pacchetti pronti per la produzione 
##E in caso caricare l'immagine sia su dockehub che su Hugginface

name: Continuous Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker Image
        run: |
          docker build -t twitter-sentiment-api:latest .

      # Esempio di push su Docker Hub (richiede Secrets nel repository)
      # - name: Login to DockerHub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      # - name: Push to DockerHub
      #   run: docker push my-user/twitter-sentiment-api:latest

      - name: Deploy Notification
        run: echo "Deployment completato con successo su main."
